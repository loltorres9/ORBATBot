discord.py>=2.3.2
# Used directly by cogs/admin.py for the Railway API call behind /restart.
# discord.py pulls it in too, but declare it so /restart doesn't depend on that.
aiohttp>=3.9.0
gspread>=5.12.0
google-auth>=2.23.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
