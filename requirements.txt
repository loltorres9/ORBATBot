discord.py>=2.3.2
# Used directly by cogs/admin.py for the Railway API call behind /restart, and by
# web/auth.py for the OAuth2 token exchange.
# discord.py pulls it in too, but declare it so neither depends on that.
aiohttp>=3.9.0
gspread>=5.12.0
google-auth>=2.23.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
# Web UI (web/). Optional at runtime — bot.py skips the site when it isn't
# configured — but installed by default, so turning it on is only a matter of
# setting the environment variables.
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.0
itsdangerous>=2.1.0
python-multipart>=0.0.9
